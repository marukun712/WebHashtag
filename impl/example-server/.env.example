SERVER_HOST=localhost:3000
SERVER_NAME=Example Tag Server

# "open" or "closed"
MODE=open

# Required when MODE=closed. Generate by running the server once without this set.
SECRET_KEY=

# Comma-separated list of allowed tag names. If unset, all tags are accepted.
TAGS=
